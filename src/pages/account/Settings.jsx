import React from "react";

const Settings = () => {
  return (
    <div className="settings-card">
      <h2>Settings</h2>
      <p>
        This page stays intentionally minimal, similar to the class example,
        and could be expanded with toggles or preferences later.
      </p>
    </div>
  );
};

export default Settings;
